{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="mb-5">
        <a href="{{ path('admin_user_index') }}">
            <button class="custom-button">
                <i class="fas fa-backspace"></i> Retour
            </button>
        </a>
    </div>
    {{ include('admin/user/include/_form.html.twig') }}
{% endblock %}
